import {PostAttachment} from "@prisma/client";

interface PostAttachmentFormProps {
    postAttachment: PostAttachment[];
}

export const PostAttachmentForm = ({
                                       postAttachment
                                   }: PostAttachmentFormProps) => {
    return (
        <div>

        </div>
    )
};